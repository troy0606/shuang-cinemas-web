{
  "name": "shuang-cinemas-web",
  "version": "1.0.0",
  "description": "shuang cinamas web demo site repo ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "dev:front": "pnpm --filter=./apps/front-stage run dev",
    "dev:back": "pnpm --filter=./apps/back-stage run dev",
    "build:apps": "pnpm --filter=./apps/* run build",
    "build:libs": "pnpm --filter=./libs/* run build && pnpm install"
  },
  "author": "Benson",
  "license": "ISC",
  "engines": {
    "node": "18.17.1",
    "pnpm": "8.5.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.45.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.3",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "nx": "^17.2.5",
    "prettier": "^3.0.2",
    "typescript": "^5.0.2"
  }
}
